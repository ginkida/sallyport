{
  "name": "sallyport-extension",
  "version": "0.24.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:browser": "npm run build && node scripts/test-browser.mjs",
    "test:e2e": "npm run build && node scripts/test-browser.mjs --mcp",
    "lint": "eslint src test",
    "format": "prettier --write src test scripts",
    "format:check": "prettier --check src test scripts"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/chrome": "^0.2.2",
    "@vitest/coverage-v8": "^4.1.9",
    "esbuild": "^0.28.1",
    "eslint": "^9.0.0",
    "prettier": "^3.9.4",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.62.1",
    "vitest": "^4.1.8"
  }
}
